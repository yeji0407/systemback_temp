language: cpp
sudo: required
dist: bionic
compiler:
- gcc
os:
- linux
before_install:
- echo $GH_TOKEN
- sudo apt-get update -qq
- sudo apt-get install debhelper libblkid-dev libmount-dev libparted-dev qtbase5-dev
  qttools5-dev-tools
script:
- sudo dpkg-buildpackage --no-sign
deploy:
    provider: releases
    api_key: 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
    file: ../*.deb
    skip_cleanup: true
    draft: true
